# Example: Flutter Desktop

Suggested structure:

```text
lib/update/update_service.dart
lib/update/update_manifest.dart
lib/update/version_comparator.dart
lib/update/update_dialog.dart
```

Start with a manual update notice. Add automatic installation later only after signing and release process are stable.
