# 06. Phase 3: Auto-Updater

Automatic updates should only be added when the release process is stable.

Before auto-updating, make sure releases are signed or verified, installers are tested, rollback is planned and update failures are handled.

Never execute arbitrary remote code just because a server says so.
