# 09. API Clients

API-based apps need special update planning because the app may break when an external API changes.

Examples include weather apps, finance apps, map apps, AI clients, shop clients and SaaS companion apps.

Add fields such as `apiCompatibilityVersion`, `minimumClientVersion` and `remoteConfigUrl` when needed.

Do not expose API secrets in examples or shipped apps.
